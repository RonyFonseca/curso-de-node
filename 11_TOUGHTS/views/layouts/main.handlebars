<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/styles.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <link rel="shortcut icon" href="/img/favicon.ico" type="image/x-icon">
    <title>Toughts</title>
</head>
<body>
    <nav id="navbar">
        <a href="/" id="logo">
            <img src="/img/toughts_logo.png" alt="toughts">
            <h1>{{session.Name}}</h1>
        </a>
        <ul>
            <li><a href="/"><i class="bi bi-house-fill"></i>Pensamentos</a></li>
            {{#if session.UserId}}
                <li><a href="/toughts/dashboard"><i class="bi bi-table"></i>Dashboard</a></li>
                <li><a href="/logout"><i class="bi bi-box-arrow-right"></i>Sair</a></li>
            {{else}}
                <li><a href="/login"><i class="bi bi-box-arrow-left"></i>Entrar</a></li>
                <li><a href="/register"><i class="bi bi-person-fill-add"></i>Registrar</a></li>
            {{/if}}
        </ul>
    </nav>
    <div class="container">
        <!--MENSAGENS-->
            {{#if message}}
                <div class="message">{{message}}</div>
            {{/if}}
        
        {{{body}}}
    </div>
    <footer>
        <p><span>Toughts</span> &copy; 2021</p>
    </footer>
</body>
</html>